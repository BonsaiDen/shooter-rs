[package]
name = "shooter-server"
version = "0.1.0"
authors = ["Ivo Wetzel <ivo.wetzel@googlemail.com>"]

[dependencies]
clap = "1.4.0"
cobalt = "0.5.2"
shared = { version = "0.1.0", path = "../shared" }

